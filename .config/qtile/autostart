#!/bin/sh

if [ $XDG_SESSION_TYPE="x11" ]
then
    # xrandr # TODO add proper refresh rate settings
    xset r rate 250 45
    setxkbmap -option ctrl:swapcaps
    picom --config ~/.config/picom/picom.conf --vsync &
fi

mullvad-vpn &
blueman-applet &
nm-applet &
flameshot &
# dunst &
